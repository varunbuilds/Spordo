# Training Requirements (Optional - only needed for model training)
# These are NOT needed for running the live app with rule-based classification
# Only install if you want to train the LSTM model on the cricket dataset

# Core ML/DL Libraries
tensorflow>=2.13.0
scikit-learn>=1.3.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Progress bars
tqdm>=4.65.0

# Already in main requirements.txt (but listed here for reference)
# opencv-python>=4.8.0
# mediapipe>=0.10.0
# numpy>=1.24.0

# Note: If you get dependency conflicts, create a separate environment:
# python -m venv train_env
# source train_env/bin/activate
# pip install -r requirements_training.txt
